#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;

int missingNums(vector<int>& arr){
    
}

int main(){

    vector<int> arr = {3,0,1};
    int n = arr.size();
    sort(arr.begin(), arr.end());
    return 0;
}